# Phase 9: Loops -> iterators

**Goal:** Replace manual `for` / `while` loops with iterator chains where idiomatic.

## Tasks

- [x] 9.1 Replace manual `for` / `while` loops with iterator chains where idiomatic

## Acceptance Criteria

**Verify:** `cargo test && cargo run -- example.log`

## Dependencies

- None (independent phase)

## Implementation Notes

**Hint:** `src/lib.rs:76` — `// подсказка: можно обойтись итераторами`
