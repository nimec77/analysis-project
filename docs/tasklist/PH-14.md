# Tasklist: PH-14 — Naming improvements

**Status:** IMPLEMENT_STEP_OK
**Ticket:** PH-14
**PRD:** `docs/prd/PH-14.prd.md`
**Plan:** `docs/plan/PH-14.md`

---

## Context

Phase 14 is a pure renaming refactor within `src/parse.rs`. Five identifiers are renamed to improve naming consistency and align with established conventions (e.g., nom naming). No behavior changes, no new dependencies, no files other than `src/parse.rs` are modified.

---

## Tasks

- [x] **1. Rename `do_unquote()` to `unquote_escaped()`**
  Rename the function definition, update the call site in `Unquote::parse()`, and update the doc comment at the definition site.
  - **AC1:** Zero occurrences of `do_unquote` remain in `src/parse.rs` (excluding occurrences that are part of `do_unquote_non_escaped`).
  - **AC2:** The call site in `Unquote::parse()` calls `unquote_escaped(input)`.

- [x] **2. Rename `do_unquote_non_escaped()` to `unquote_simple()`**
  Rename the function definition, update the call site in `QuotedTag::parse()`, update 3 test references, rename the test function `test_do_unquote_non_escaped` to `test_unquote_simple`, and update the intra-doc link from `[do_unquote]` to `[unquote_escaped]` in the doc comment.
  - **AC1:** Zero occurrences of `do_unquote_non_escaped` remain in `src/parse.rs`.
  - **AC2:** The intra-doc link in the `unquote_simple` doc comment references `[unquote_escaped]`.
  - **AC3:** The test function is named `test_unquote_simple`.

- [x] **3. Rename `All` struct to `Tuple`**
  Rename the struct definition, all `Parser` trait impls, all type annotations in `Parsable` return types, and update doc comments referencing `all` to reference `tuple`.
  - **AC1:** Zero occurrences of `struct All` or `All<` remain in `src/parse.rs`.
  - **AC2:** The doc comment reads `` `tuple` `` (not `` `all` ``) when referencing the nom analog.
  - **AC3:** All 14 occurrences are updated to use `Tuple`.

- [x] **4. Rename `all2()` to `tuple2()`**
  Rename the function definition and all 8 call sites. Update the doc comment to reference `[Tuple]` instead of `[All]`.
  - **AC1:** Zero occurrences of `all2` remain in `src/parse.rs`.
  - **AC2:** The doc comment references `[Tuple]` instead of `[All]`.
  - **AC3:** All 8 call sites use `tuple2(...)`.

- [x] **5. Rename `stdp` module to `primitives`**
  Rename the module declaration and all 38 qualified references (`stdp::U32`, `stdp::I32`, `stdp::Byte`).
  - **AC1:** Zero occurrences of `stdp` remain in `src/parse.rs`.
  - **AC2:** All qualified references use `primitives::U32`, `primitives::I32`, `primitives::Byte`.

- [x] **6. Verify all renames and run tests**
  Run `cargo test` and `cargo run -- example.log` to confirm no regressions. Verify zero occurrences of old names remain. Verify zero new compiler warnings.
  - **AC1:** `cargo test` passes all tests (no test cases deleted).
  - **AC2:** `cargo run -- example.log` output is identical to pre-phase output.
  - **AC3:** `grep` for old names (`All<`, `all2`, `stdp`, `do_unquote`) in `src/parse.rs` returns zero matches.
  - **AC4:** No new compiler warnings.
